package mk.ukim.finki.konsultacii;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import java.util.stream.Collectors;

public class StreamTests {

    public static void main(String[] args) {
//        List<Integer> integer1 = Arrays.asList(1,2,3);
//        List<Integer> integer2 = Arrays.asList(3,4);
//
//        List<List<Integer>> results = new ArrayList<>();
//
//        for (Integer i : integer1) {
//            for (Integer j : integer2) {
//                results.add(Arrays.asList(i,j));
//            }
//        }
//
//        results = integer1.stream().flatMap(i -> integer2.stream().map(j -> Arrays.asList(i,j))).collect(Collectors.toList());
//        System.out.println(results);
    }
}
